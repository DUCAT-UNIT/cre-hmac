# DUCAT Gateway Server Configuration
# Copy this file to .env and fill in your values

# Required: CRE workflow ID
CRE_WORKFLOW_ID=00392b2b81bb38767380b8c7d8e39d88b8d4293505caa9831f9fc4baab68ea3f

# Required: Your Ethereum address that's authorized to call the CRE gateway
DUCAT_AUTHORIZED_KEY=0x5b3ebc3622dd75f0a680c2b7e4613ad813c72f82

# Required: Your Ethereum private key (without 0x prefix or with it)
DUCAT_PRIVATE_KEY=your_private_key_here

# Required: Public callback URL where CRE will send webhook callbacks
# For development, use ngrok: ngrok http 8080
# Then set this to: https://your-subdomain.ngrok.app/webhook/ducat
GATEWAY_CALLBACK_URL=https://your-ngrok-url.ngrok.app/webhook/ducat

# Optional: CRE gateway URL (defaults to zone-a gateway)
# CRE_GATEWAY_URL=https://01.gateway.zone-a.cre.chain.link

# Optional: How long to hold HTTP connection waiting for webhook (in seconds)
# Default: 60 seconds
# BLOCK_TIMEOUT_SECONDS=60

# Optional: How often to clean up old completed/timed-out requests (in seconds)
# Default: 120 seconds (2 minutes)
# CLEANUP_INTERVAL_SECONDS=120

# Optional: Maximum number of concurrent pending requests
# Default: 1000
# Prevents memory exhaustion under heavy load
# MAX_PENDING_REQUESTS=1000

# Optional: Logging configuration
# LOG_LEVEL options: debug, info, warn, error (default: info)
# LOG_FORMAT options: json, console (default: console)
# LOG_LEVEL=info
# LOG_FORMAT=console

# Optional: Server port (default: 8080)
# PORT=8080
